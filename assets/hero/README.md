# Hero (optional)

The homepage hero currently uses an original generated SVG/CSS
composition (the "threshold" motif + a soft light field) instead of a
photo, so nothing is required here. If a real hero photo or video is ever
preferred instead, drop it here and wire it up in
`src/components/sections/Hero.astro`.
