# CV

Add the CV file here as:

```
amal-alsofayan-cv.pdf
```

Both "Download CV" and "View Full CV" in the Contact section point to
this exact file — Download uses the browser's native download behavior
(via the `download` attribute), View opens it in a new tab.
